export type Children = {
    children: React.ReactNode;
};

export type NowPlayingData = {
    isPlaying: boolean;
    title: string;
    album: string;
    artist: string;
    albumImageUrl: string;
    songUrl: string;
}